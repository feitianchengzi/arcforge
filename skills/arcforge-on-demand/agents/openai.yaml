interface:
  display_name: "ArcForge On Demand"
  short_description: "显式调用后查询、校验并加载 catalog 技能。"
  default_prompt: >-
    使用 $arcforge-on-demand 执行本次显式调用：按指定名称或任务意图，从最小元数据语义选择，再 exact resolve 并读取一个技能。遵守调用方查询范围，返回技能路径和用途；不自行触发后续发现，不执行安装或治理写入。
policy:
  allow_implicit_invocation: false
