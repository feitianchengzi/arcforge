import { contextBridge, ipcRenderer } from "electron";

contextBridge.exposeInMainWorld("skillops", {
  chooseWorkspace: () => ipcRenderer.invoke("workspace:choose"),
  scanWorkspace: (root: string) => ipcRenderer.invoke("workspace:scan", root),
  saveConfig: (root: string, config: unknown) => ipcRenderer.invoke("workspace:saveConfig", root, config),
  openWorkspaceFolder: (root: string) => ipcRenderer.invoke("workspace:openFolder", root),
  getDefaultTargets: () => ipcRenderer.invoke("system:defaultTargets"),
  getEnvironmentStatus: () => ipcRenderer.invoke("system:environment"),
  installCli: () => ipcRenderer.invoke("system:installCli"),
  openExternal: (url: string) => ipcRenderer.invoke("system:openExternal", url),
  loadAppState: () => ipcRenderer.invoke("appState:load"),
  saveAppState: (patch: unknown) => ipcRenderer.invoke("appState:save", patch),
  migrateAppState: (legacyState: unknown, origin: string) => ipcRenderer.invoke("appState:migrate", legacyState, origin),
  addRemoteWorkspace: (remoteUrl: string) => ipcRenderer.invoke("workspace:addRemote", remoteUrl),
  sourceUpdateStatus: (root: string) => ipcRenderer.invoke("source:status", root),
  updateSource: (root: string, confirm?: boolean) => ipcRenderer.invoke("source:update", root, confirm),
  createMergePlan: (options: unknown) => ipcRenderer.invoke("merge:plan", options),
  mergeIntoProject: (options: unknown) => ipcRenderer.invoke("merge:run", options),
  listAppliedSources: (root: string) => ipcRenderer.invoke("applied:list", root),
  driftAppliedSources: (root: string, id?: string) => ipcRenderer.invoke("applied:drift", root, id),
  runAppliedSources: (root: string, id?: string) => ipcRenderer.invoke("applied:run", root, id),
  applyFromSource: (root: string, from: string | undefined, profile: string, targetDir: string, save?: boolean, skills?: string[]) => ipcRenderer.invoke("apply:run", root, from, profile, targetDir, save, skills),
  driftFromSource: (root: string, from: string | undefined, profile: string, targetDir: string, skills?: string[]) => ipcRenderer.invoke("apply:drift", root, from, profile, targetDir, skills),
  createSharePlan: (root: string, remoteUrl: string, visibility: "private" | "public", targetMode: "direct" | "namedProject", projectName: string, profileName: string, message?: string, delivery?: string, branch?: string, sameRepository?: boolean, sameRepositoryRemote?: string) => ipcRenderer.invoke("share:plan", root, remoteUrl, visibility, targetMode, projectName, profileName, message, delivery, branch, sameRepository, sameRepositoryRemote),
  shareProject: (root: string, remoteUrl: string, visibility: "private" | "public", message: string, targetMode: "direct" | "namedProject", projectName: string, profileName: string, delivery?: string, branch?: string, confirm?: boolean, sameRepository?: boolean, sameRepositoryRemote?: string) => ipcRenderer.invoke("share:run", root, remoteUrl, visibility, message, targetMode, projectName, profileName, delivery, branch, confirm, sameRepository, sameRepositoryRemote),
  shareDriftReport: (root: string, remoteUrl: string, targetMode: "direct" | "namedProject", projectName: string, profileName: string, sameRepository?: boolean, sameRepositoryRemote?: string) => ipcRenderer.invoke("share:drift", root, remoteUrl, targetMode, projectName, profileName, sameRepository, sameRepositoryRemote),
  openDriftDiff: (report: unknown) => ipcRenderer.invoke("diff:openDrift", report),
  listSkillFiles: (root: string, skillPath: string) => ipcRenderer.invoke("skillFile:list", root, skillPath),
  listWorkspaceFiles: (root: string, directoryPath: string) => ipcRenderer.invoke("skillFile:listWorkspace", root, directoryPath),
  readSkillFile: (root: string, filePath: string) => ipcRenderer.invoke("skillFile:read", root, filePath),
  writeSkillFile: (root: string, filePath: string, content: string) => ipcRenderer.invoke("skillFile:write", root, filePath, content),
  openSkillFileWindow: (root: string, skillPath: string, filePath?: string, context?: unknown) => ipcRenderer.invoke("skillFile:openWindow", root, skillPath, filePath, context),
  openWorkspaceFileWindow: (root: string, directoryPath: string, filePath?: string, context?: unknown) => ipcRenderer.invoke("skillFile:openWorkspaceWindow", root, directoryPath, filePath, context)
});
